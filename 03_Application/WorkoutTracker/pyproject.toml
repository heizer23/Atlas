[project]
name = "workout-tracker"
version = "0.2.0"
description = "WorkoutTracker backend — Dataset API, psycopg2 pool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "psycopg2-binary",
    "pydantic",
]

[tool.setuptools]
packages = ["backend"]
