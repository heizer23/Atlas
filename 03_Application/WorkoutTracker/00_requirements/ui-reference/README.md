
  # Workout Detail View — Design Reference Only

  **This is reference-only material and is not part of the running WorkoutTracker application.**
  No live application code imports from this directory.

  This is a Figma-to-code design reference export (shadcn/ui components) used during initial WorkoutTracker design.
  The original Figma project is available at https://www.figma.com/design/qYUTjcCzOXzOnV9fIdI9nY/Workout-Detail-View.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
